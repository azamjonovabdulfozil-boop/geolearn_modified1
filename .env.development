# Lokal ishlab chiqish: bo'sh qoldirilgan — /api so'rovlari vite proxy orqali
# VITE_API_TARGET (default http://localhost:3001) ga yo'naltiriladi.
# Bu fayl .env dan ustun turadi, shuning uchun dev rejimda har doim lokal backend ishlatiladi.
VITE_API_URL=https://geolearn-modified1.onrender.com
